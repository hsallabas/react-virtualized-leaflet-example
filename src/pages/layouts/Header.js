import React from 'react'

const Header = () => {
    return (
        <header className="header">
            <div className="d-flex">
                header
            </div>
        </header>
    );
};

export default Header;